// Generated by CoffeeScript 1.4.0

/*
  Let's build graph with director and load results into
  graph.  Also verify that we have an appropriate _builder
  to load the graph with.
*/


(function() {
  var Db,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Db = (function() {
    /*
        Method: constructor
    
        Initialize Db with a reference to where relation, schema, and 
        dbSettings files are.
    */

    function Db(relationPath, schemaPath, dbSettingsPath) {
      this.loadBuilder = __bind(this.loadBuilder, this);

      this.loadConfigs = __bind(this.loadConfigs, this);
      this._relationPath = relationPath;
      this._schemaPath = schemaPath;
      this._dbSettings = dbSettingsPath;
    }

    Db.prototype.loadConfigs = function(callback) {};

    Db.prototype.loadBuilder = function(_builder) {};

    return Db;

  })();

  module.exports = Db;

}).call(this);
